import { Router } from '~/components/router/Router'
import { useEffect } from 'react'

function Main() {
    useEffect(() => {
    }, [])
    return (
        <main>
            <Router />
        </main>
    )
}

export default Main
